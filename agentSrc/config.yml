WebSocketConfig:
  # https://statics.teams.cdn.office.net/evergreen-assets/safelinks/1/atp-safelinks.html
  # this is to communicate with the redis outside the localhost. i.e if you don't want a redis container.
  host: localhost
  port: 8080
  path: /read
  scheme: ws

SocketConfig:
  network: tcp
  host: 127.0.0.1
  port: 5555

Buffer: 512